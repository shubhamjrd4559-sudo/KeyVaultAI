import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Minimal production-ready config.
  // Add environment-specific options here as the project grows.
};

export default nextConfig;
